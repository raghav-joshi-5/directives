import { EventdirDirective } from './eventdir.directive';

describe('EventdirDirective', () => {
  it('should create an instance', () => {
    const directive = new EventdirDirective();
    expect(directive).toBeTruthy();
  });
});
