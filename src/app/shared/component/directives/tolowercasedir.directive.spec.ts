import { TolowercasedirDirective } from './tolowercasedir.directive';

describe('TolowercasedirDirective', () => {
  it('should create an instance', () => {
    const directive = new TolowercasedirDirective();
    expect(directive).toBeTruthy();
  });
});
