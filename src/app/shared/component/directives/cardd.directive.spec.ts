import { CarddDirective } from './cardd.directive';

describe('CarddDirective', () => {
  it('should create an instance', () => {
    const directive = new CarddDirective();
    expect(directive).toBeTruthy();
  });
});
